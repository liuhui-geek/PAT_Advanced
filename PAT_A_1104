#include<iostream>
#include<cctype>
#include<algorithm>
//1104
using namespace std;

int main() {
	double n = 0, ans = 0;
	double num = 0;
	cin >> n;
	for (int i = 0; i < n; i++) {
		scanf("%lf", &num);
		ans += num * (i + 1)*(n - i);
	}
	printf("%.2f", ans);
	return 0;
}
